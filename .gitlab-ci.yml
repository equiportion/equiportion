#
# CI
#
image: node:latest

stages:
- build
- test

build-npm-dev:
  stage: build
  script:
    - npm install
  tags: 
    - checker-2
  artifacts:
    paths:
      - node_modules/

test-eslint:
  stage: test
  needs:
    - build-npm-dev
  dependencies:
    - build-npm-dev
  script:
    - npm run lint
  tags:
    - checker-2

test-cypress:
  stage: test
  needs:
    - build-npm-dev
  dependencies:
    - build-npm-dev
  script:
    - npx cypress run
  tags:
    - checker-2